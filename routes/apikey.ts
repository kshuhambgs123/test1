import express, { Request, Response } from 'express';

const app = express.Router();


app.post("searchLeads", async (req: Request, res: Response): Promise<void> => {

});


export default app;